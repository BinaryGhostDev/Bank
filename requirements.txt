# Python Dependencies
mysql-connector-python>=8.0.0
# Add any additional Python dependencies your project may require

# MySQL Setup Commands
# Add commands for setting up MySQL, if necessary
# For example, if you are using MySQL Server, you might want to include:
# CREATE DATABASE IF NOT EXISTS bank;
# GRANT ALL PRIVILEGES ON bank.* TO 'your_username'@'localhost' IDENTIFIED BY 'your_password';
# FLUSH PRIVILEGES;
